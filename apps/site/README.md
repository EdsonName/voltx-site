# Site público e área do cliente — VoltX

Reservado à aplicação Next.js + TypeScript do site público e da área do cliente. Consome a API VoltX; não deve conter o painel administrativo nem acesso direto ao PostgreSQL.

Referências: [Arquitetura](../../docs/ARQUITETURA.md).

Nesta etapa existe apenas a estrutura de diretórios documentada. Nenhuma aplicação, pacote ou configuração executável foi inicializada.
