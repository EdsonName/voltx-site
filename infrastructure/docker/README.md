# Infraestrutura Docker — VoltX

Reservado aos arquivos específicos da infraestrutura Docker quando sua implementação for iniciada. Não deve armazenar volumes, dumps, dados persistentes reais ou segredos no Git.

Referências: [Arquitetura](../../docs/ARQUITETURA.md).

Nesta etapa existe apenas a estrutura de diretórios documentada. Nenhuma aplicação, pacote ou configuração executável foi inicializada.
